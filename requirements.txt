# --- UI (runnable now) ---
streamlit>=1.40
pydantic>=2.7

# --- Backend / crew (uncomment as agents, RAG, and tools come online) ---
# crewai>=0.80
# chromadb>=0.5
# rank-bm25>=0.2          # keyword index alongside Chroma
# tavily-python>=0.5      # guardrailed web fallback
# pint>=0.24              # deterministic unit math (culinary-math tool)
# anthropic>=0.40         # Claude via CrewAI's LLM layer
